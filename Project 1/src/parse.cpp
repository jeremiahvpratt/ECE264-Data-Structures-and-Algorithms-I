/*
Jeremiah Pratt
Program Parses written commands into 16 bit hex commands, separated by commas,
to make testing for Kirtman final project easier.
Commands should be contained in individual lines of a text file,
Four words each line, separated by spaces, with no punctuation.
Valid commands are as follows:

NOP, ADD, SUB, AND, IOR, XOR, RRL, RRR, NOT, MOV, LOD, STO, TSC, TSS, JMP, JSR, RET

The next three words should consist of 4 digit addresses.
*/